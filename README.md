# Student_Mentor

so this  project 
dksgkrdlpgkdrplbm,dl;mb,;ldb,;l

| Filename | Language | Description |
| :--- | :--- | :--- |
| `server.js` | JavaScript | Node.js (Express) backend to manage API routes, handle authentication (bcrypt), and spawn C++ executables. |
| `index.html` | HTML/JS/CSS | Frontend interface for interacting with the bank system. Contains all client-side logic for API calls. |
| `store_account.cpp` | C++ | C++ logic for adding or updating account details in `accounts.txt`. |
| `read_accounts.cpp` | C++ | C++ logic to read and output all accounts from `accounts.txt`. |
| `delete_account.cpp` | C++ | C++ logic to remove an account from `accounts.txt` using a temporary file method. |
| `search_account.cpp` | C++ | C++ logic for all search modes (number, name, fuzzy, type, range). |
| `transaction_manager.cpp` | C++ | C++ logic for deposit, withdraw, viewing history, and undoing transactions. |
